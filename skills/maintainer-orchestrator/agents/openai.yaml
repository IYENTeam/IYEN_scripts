interface:
  display_name: "Maintainer Orchestrator"
  short_description: "Coordinate one verified project lane per repository"
  default_prompt: "Use $maintainer-orchestrator to coordinate one project lane per repository, preserve work, keep support subagents read-only, enforce proof gates and exact-head CI, capture decisions, maintain dependencies, and prepare authorized releases."
